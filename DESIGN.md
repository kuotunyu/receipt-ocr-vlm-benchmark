# 設計文件

本文件記錄關鍵技術選型的理由與取捨，特別是那些「一開始以為會怎樣，實測後發現不是那樣」的
決策——這類決策比「選了最新最貴的模型」更能反映工程判斷。

## 1. Schema 與正規化：先凍結規則，再比公平

`schema/invoice_schema.json` 在寫任何管線程式碼之前就先定案（Phase 0），理由：如果先寫 Pipeline A
再回頭定 schema，schema 很容易不自覺地向 Pipeline A 的輸出習慣靠攏（例如 Pipeline A 若習慣把統編
留白就填 `""` 而非 `null`，schema 若照抄就會讓 Pipeline B 的 `null` 輸出被誤判為格式錯誤）。

`src/common/normalize.py` 把「格式層面的寬容」全部收斂在同一個模組，兩條管線與 ground truth 在比對
前都要經過它：民國/西元日期、全形/半形、金額千分位、統編/發票號碼格式都在這裡統一。**無法解析一律
回傳 `null`，由 eval 記為錯誤**——這個決定是刻意的：如果 normalize 對「猜測性」的格式做容錯（例如
把「不確定」硬轉成某個值），兩條管線比的就不是「有沒有正確理解文件」，而是「誰的輸出格式恰好比較
討 normalize 喜歡」。

`seller_name` 額外欄位（原始 schema 沒有，使用者要求加入）：這是刻意選的錯誤分析素材——店名常是
logo 特殊字體，傳統 OCR 對美術字很弱、VLM 則能靠上下文合理猜測，兩條管線在這個欄位上的表現差異
本身就是故事。

## 2. Pipeline A：前處理選型

### deskew：minAreaRect，而非 Hough

用 Otsu 反相二值化取前景像素、取 `cv2.minAreaRect` 的角度，而非 Hough 直線偵測。理由：真實收據
不保證有整齊的表格框線可供 Hough 抓取（尤其手寫收據、皺摺收據），但文字像素的整體外接矩形角度
對「文件本身傾斜」這件事是穩健的訊號，不依賴特定版面元素存在與否。角度換算公式（`angle < -45` 則
`+90`）已用已知旋轉角度（12°）的合成測試圖校準過方向，見 `src/pipeline_a/preprocess.py`。

**實測發現（超出預期）**：deskew 對 PP-OCRv6 的**原始文字辨識**幫助有限——偵測模型本身對中度旋轉
（12°）就有一定容忍度。deskew 真正的價值在 `layout.py` 的**版面重建**：沒有 deskew 時，同一列
文字的 y 座標會隨 x 位置飄移，導致列分組邏輯把不同列的內容錯誤合併（例如把表頭「品名」跟另一列的
金額「60」合併成同一個假品項）。也就是說，**deskew 保護的是下游的結構化邏輯，不是 OCR 辨識率本身**
——這個區分在寫 EVAL_REPORT 時很重要，不能簡單歸因成「OCR 準確率」的差異。

### 去噪：fastNlMeansDenoising

選 Non-local Means 而非簡單的高斯/中值模糊，因為中文字筆畫細節多，簡單模糊容易讓相近筆畫糊在一起；
NLM 對重複紋理（如紙張纖維雜訊）效果較好，同時盡量保留邊緣。

### 二值化：adaptive Gaussian threshold，且刻意不預設為「必開」

用 `cv2.adaptiveThreshold`（Gaussian 加權局部閾值）而非全域 Otsu，因為真實收據常見局部光影不均
（手機拍照常見）。

**實測發現（推翻了原本的假設）**：二值化不是無條件有益。在雜訊+模糊的合成測試圖上，加了二值化的
結果反而比不加更差（統編辨識錯誤、總計欄位抓空）；在褪色（低對比）的測試圖上，二值化則明顯有幫助。
推測原因：PP-OCRv6 的辨識模型是深度學習模型，訓練資料多為自然灰階/彩色影像，強制二值化會把輸入
推出訓練分布之外——這對「為傳統 template-matching OCR 設計」的古典二值化技術而言是個真實的相容性
問題。**結論**：二值化應該是消融實驗裡的一個可調開關（`PreprocessConfig.binarize`），而非寫死開啟
的固定步驟；正式產品化時應該考慮依影像品質分類（雜訊 vs 褪色）動態決定是否二值化，而非一視同仁。

### OCR 引擎：PaddleOCR 3.7.0 / PP-OCRv6

選擇理由：單一模型原生同時支援繁體中文、簡體中文、英文、日文，不需要為繁中另外找模型或做二次
訓練；官方預設已是 PP-OCRv6（比原規劃的 PP-OCRv5 更新）。Windows 上刻意選 CPU 版而非 GPU 版
（GPU 版在 Windows 的安裝複雜度顯著高於 Linux，且 CPU 推論的絕對延遲雖然較高，仍在可接受範圍）。

**已知限制**：本機因 PaddlePaddle 3.3.1 在 Windows CPU 上的新版 PIR 執行器 bug，必須關閉 MKLDNN
（`enable_mkldnn=False`）才能跑，這犧牲了 CPU 推論加速——這代表本專案量到的 Pipeline A 延遲數字
**不代表 PP-OCRv6 在正常（Linux + MKLDNN）環境下的真實吞吐量**，EVAL_REPORT 對此有明確揭露。

### Layout 分析：座標分行，而非版面模型

沒有引入額外的版面分析模型（如 PP-StructureV3），而是用簡單的 y 座標動態容忍度分組 + x 座標排序。
理由：發票/收據的版面結構相對簡單（大致是一行行文字，頂多品項有直欄），一個重量級版面模型對這個
任務是不成比例的複雜度；`layout.py` 純函式、不需要跑 OCR 引擎就能單元測試（17 個測試案例），開發
與除錯成本低很多。

### 品項抽取：個別 OCR box 順序，而非整行字串

品項的名稱/金額切分用「同一列裡個別 box 依左右順序」而非「整行字串再用正則切」——因為中文字之間
沒有空白，字串層級很難分辨「品名」跟緊接著的數字是不是同一欄；但 OCR 偵測階段本來就會把視覺上分開
的文字切成不同的 box（名稱、數量、金額通常是三個獨立的框），拿這個現成的幾何切分來用最可靠。

### LLM 補漏：只在規則完全抓不到品項時才觸發

`assemble.py` 預設先跑規則抽取，**只有規則抓到 0 筆品項時**才呼叫本地 `qwen3:4b` 補漏，而非每張都跑
LLM 再跟規則結果比較。理由：保持「傳統管線」的敘事乾淨（成本對比才有意義），也避免規則已經抽對時
被 LLM 的幻覺覆蓋掉。`OllamaUnavailable` 例外會被優雅捕捉，退回等同 `--no-llm` 的行為。

## 3. Pipeline B：VLM 選型

| Backend | 選擇理由 |
|---|---|
| Qwen3-VL-8B-Instruct（本地，Ollama） | 2025-10 發布、取代 Qwen2.5-VL；8B 量化後 12GB VRAM 可跑；vLLM 不支援 Windows 原生，改用 Ollama serving |
| Gemini 3.5 Flash | 2026-05 發布，成本效率代表（$1.5/$9 每百萬 token） |
| OpenAI GPT-5.4-nano | 官方定位就是速度/成本優先的分類與資料抽取任務（$0.2/$1.25 每百萬 token），與 Gemini Flash 同級距、比較不同生態系的抽取品質差異（原規劃是 Claude Sonnet 5 當「文件理解上限代表」，中途應使用者要求改為 OpenAI，成本軸的張力因此變小，但換來「同價位不同供應商」的對比角度） |

三個 backend 共用同一個 `VLMBackend` 抽象類別（`vlm_base.py`），只需各自實作 `_call()`（送一次請求、
回傳原始文字），JSON 解析、schema 驗證、重試、用量統計全部收斂在基底類別——確保「結構化輸出穩定性」
三者用同一把尺量，不會因為某個 backend 的重試邏輯寫得比較寬鬆而佔到便宜。

### Prompt 設計：欄位說明直接從 schema 動態組出

`prompts.py` 的 `_field_descriptions()` 直接讀 `schema/invoice_schema.json` 每個欄位的 `description`
組進 prompt，不另外維護一份文字說明。好處：改 schema（例如新增欄位）時 prompt 自動跟著更新，不會
出現「schema 改了但 prompt 忘記改」的不同步問題。

### 結構化輸出穩定性：解析 → 驗證 → 帶錯誤重試

```
呼叫 VLM → 嘗試抽出 JSON 物件 → 正規化 → schema 驗證
  ├─ 驗證通過 → 回傳（記錄用了幾次嘗試）
  └─ 驗證失敗 → 把上次的原始回應 + 具體錯誤訊息附進下一次 prompt → 重試（預設上限 2 次）
```

重試只處理「JSON 格式/schema 不合法」這類結構性問題，**底層連線/認證錯誤（金鑰錯、服務打不通）
會直接往上拋出**，不會被誤判成「值得重試」而浪費 API 額度或掩蓋真正的環境問題。這個區分在
`scripts/smoke_test_pipeline_b.py`／`run_eval.py` 都靠它做「backend 是否可用」的判斷。

實測驗證：本地 qwen3-vl:8b 在 5 張合成圖上，2/5 觸發過一次重試都成功恢復——證明這套機制在真實模型
上真的有用，不只是 mock 測試通過。

### OCR 輔助文字（`--with-ocr-hint`）：不是穩賺不賠的加分項

`extract(image_bytes, ocr_hint=...)` 支援把 Pipeline A 的 OCR 文字當輔助輸入喂給 VLM，設計動機是
「反正 OCR 文字都跑出來了，多給模型一點資訊應該只有好處」。**實測推翻了這個直覺**：在雜訊模糊圖上，
VLM 在沒有 OCR 提示時把某個字讀對了，加上 OCR 提示後反而讀錯——因為 Pipeline A 自己對那個字的判讀
本來就不穩，把不確定的文字當「提示」喂給 VLM，等於把兩條管線各自的不確定性疊加在一起，而不是互相
校正。這跟前面二值化的教訓是同一個主題：**額外資訊/前處理預設不是有幫助的，要實測才知道**。

### VLM 輸入的影像大小上限：`load_image_bytes_for_vlm`

正式雙軌評估（合成繁中 45 + SROIE 45）跑到一半，本地 Ollama 服務在處理某幾張解析度較大的
真實掃描收據時**直接崩潰**，之後所有請求都連不上。真實照片遠比合成圖（520×760）大，餵給
本地 8B 模型時記憶體/顯存需求暴增撐爆了 serving 進程。修法：`run_eval.py` 統一把所有
Pipeline B 的輸入影像等比縮到長邊 ≤1600px 才送出，三個 backend 一視同仁（維持組間可比較
性）。**這也是正式產品上傳前的標準做法**——混用不同來源/尺寸的影像時，這道防護該在第一次
跑之前就做，而非等它炸了才修。

## 4. 評估框架設計

### exact vs fuzzy：用 rapidfuzz 正規化編輯距離，閾值 0.8

`fuzzy_score` 對 `None` 做特殊處理（兩邊皆 `None` 視為滿分，只有一邊 `None` 視為 0 分，避免用字串
距離處理「缺值」這種質性差異），其餘一律轉字串算正規化 Levenshtein 相似度。閾值 0.8 選在「容忍
1-2 個字元的 OCR/VLM 常見誤讀，但不縱容整體亂猜」的區間——之後有真實 dev set 後應該重新校準。

### items：先對齊，再算 P/R/F1

品項清單沒有固定順序，直接按索引比對會讓「預測多寫/少寫一項」波及後面所有品項的比對結果。
`match_items.py` 先用名稱相似度做貪婪對齊（閾值 0.6，比欄位比對寬鬆，因為品項名稱字數少、
OCR/VLM 誤讀的相對影響比例更大），再用對齊結果算 precision/recall/f1/amount_accuracy。

**實測發現的度量盲點**：f1 只衡量「有沒有配對到」，配對到但名稱其實有錯字（如「鮪魚」被讀成
「鯖魚」）仍然算對，導致 f1 呈現「全部正確」的假象。加了 `name_exact_rate`／`name_avg_similarity`
兩個指標後才量到：qwen3-vl 在合成測試圖上 items f1 雖是 1.0，但 `name_exact_rate` 只有 0.9——這
提醒我們**報告 F1 這類彙總指標時，永遠要問「這個指標的門檻設定會不會恰好把某類錯誤蓋住」**。

### 延遲：p50/p95，且強制呼叫端處理 cold/warm

`latency.py` 不提供「懶人版」的單一平均值函式，強制呼叫端自己呼叫 `split_cold_warm` 把首次呼叫
（本地模型載入 VRAM 的一次性成本，實測 83 秒 vs 之後 10~30 秒）跟穩態延遲分開算——混在一起會嚴重
高估平均延遲。另外實測發現 Pipeline A、B 的延遲測量若在同一批次裡緊鄰著跑，Ollama 的 keep-alive
會讓模型持續佔用資源、拖慢同機的 CPU 版 PaddleOCR，導致數字不可比較——這是量測方法論教訓，而非
程式邏輯錯誤。

### 成本：API token 單價 vs 本地 GPU 時間換算

`configs/pricing.yaml` 集中管理所有價格假設（查證日期寫在檔案裡），之後任一家調價只改設定檔。
本地 backend 沒有直接的「美金成本」，用「若改租雲端 GPU 的等效租用價」換算，讓本地跟 API 兩條線
能放進同一張表比較；Pipeline A 純 CPU 運算的邊際成本則標記為近似免費，不特別建模（在小規模場景下
這個簡化是合理的，大規模部署時才需要認真估 CPU-小時成本）。

## 5. 工程挑戰（誠實記錄，非事後補充的免責聲明）

- **PaddleOCR 3.x API 與 2.x 完全不相容**：`.ocr()` 變成 deprecated wrapper，回傳格式從巢狀
  list 變成 `OCRResult` dict。開發時期才發現，說明「跟著官方最新版走」有維護成本。
- **Windows CPU 推論需要 `enable_mkldnn=False`** 才能繞過 PaddlePaddle 3.3.1 在 Windows 上的
  PIR 執行器 bug，代價是推論變慢——這類「解法是對的，但有隱藏代價」的取捨在生產環境很常見。
- **本專案資料夾路徑含中文字，導致 `cv2.imread`/`imwrite` 靜默失敗**（底層走 ANSI codepage
  fopen）。統一改用 `src/common/imageio.py` 的 `imread_unicode`/`imwrite_unicode`
  （`np.fromfile`/`tofile` + `imdecode`/`imencode`）解決。這類「路徑編碼」問題在中文使用者的
  開發環境其實很常見，卻很少在教學文章裡被提到。
