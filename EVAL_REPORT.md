# 評估報告

> 資料策略：因故未能拍照+人工標註真實台灣發票（見 `plan.md` 的「資料策略 pivot」），改採
> **雙軌零標註方案**：45 張合成繁體中文發票/收據（`data/synthetic/`，`scripts/make_synthetic_dataset.py`
> 生成，ground truth 生成當下即已知）+ 45 張 **SROIE**（ICDAR 2019 真實英文收據基準，
> `scripts/download_sroie.py` 下載轉換）。前者保住繁體中文故事線與品項評估，後者提供
> 真實照片才有的紙張皺摺/印刷瑕疵/手寫註記，並額外測出一個沒預期到的核心發現：
> **可移植性（同一套系統換一個語言/國家的文件會發生什麼事）**。
>
> 重現指令：
> ```
> .venv\Scripts\python scripts\run_eval.py --images-dir data/synthetic/raw --labels-dir data/synthetic/labels --backends ollama,openai
> .venv\Scripts\python scripts\run_eval.py --images-dir data/sroie/raw --labels-dir data/sroie/labels --ocr-lang en --no-items --backends ollama,openai
> .venv\Scripts\python scripts\make_report.py results/eval_<timestamp>/summary.json
> ```
>
> README 表格使用的去識別化 aggregate summary 已收錄於 [`results/official/`](results/official/README.md)；
> 可在不呼叫模型或網路的情況下，使用 `scripts/export_official_results.py --check` 對照本機正式結果。

## 整體對比

### 合成繁體中文 45 張

| 管線 | avg exact | avg fuzzy | items F1 | items 名稱 exact | p50（warm） | p95（warm） | JSON 有效率 | 每 100 張成本 |
|---|---|---|---|---|---|---|---|---|
| Pipeline A－有前處理 | 0.917 | 0.927 | 0.607 | 0.923 | 19.7s | 35.7s | — | 近似免費 |
| Pipeline A－無前處理 | 0.965 | 0.968 | 0.580 | 0.933 | 5.6s | 20.0s | — | 近似免費 |
| Qwen3-VL-8B（本地） | 0.971 | 0.981 | 0.970 | 0.923 | 23.3s | 40.7s | 0.978 | GPU 時間換算 |
| Qwen3-VL-8B + OCR 輔助 | **0.981** | 0.981 | 0.972 | 0.948 | 26.5s | 53.6s | 0.978 | GPU 時間換算 |
| GPT-5.4-nano | 0.660 | 0.695 | 0.823 | 0.863 | **1.8s** | 6.7s | 1.000 | **$0.04** |
| GPT-5.4-nano + OCR 輔助 | 0.908 | 0.911 | 0.914 | 0.952 | 2.0s | 6.0s | 1.000 | $0.04 |

### SROIE 真實英文收據 45 張

| 管線 | avg exact | p50（warm） | p95（warm） | JSON 有效率 | 每 100 張成本 |
|---|---|---|---|---|---|
| Pipeline A－有前處理 | **0.390** | 19.9s | 74.4s | — | 近似免費 |
| Pipeline A－無前處理 | **0.416** | 19.3s | 69.6s | — | 近似免費 |
| Qwen3-VL-8B（本地） | 0.829 | 55.2s | 148.6s | 0.956 | GPU 時間換算 |
| Qwen3-VL-8B + OCR 輔助 | 0.803 | 52.7s | 96.6s | 1.000 | GPU 時間換算 |
| GPT-5.4-nano | **0.857** | **3.6s** | 6.6s | 1.000 | **$0.10** |
| GPT-5.4-nano + OCR 輔助 | 0.857 | 3.6s | 6.7s | 0.956 | $0.11 |

（SROIE 未標註品項，items 指標不適用，見資料策略註記；items 相關結論僅取自合成繁中資料集。）

## 核心發現一：可移植性崩跌——本專案最強的單一證據

**Pipeline A 從合成繁中的 0.92~0.97 掉到 SROIE 的 0.39~0.42；VLM 只從 0.97~0.98 掉到
0.80~0.86。** 同一套規則、同一套程式碼，換一個語言/國家的收據就大幅崩潰；VLM 用同一個
prompt 幾乎原封不動就還能用七、八成。逐欄位拆解崩潰的原因非常具體：

| 欄位 | Pipeline A（合成→SROIE） | Qwen3-VL（合成→SROIE） | 解讀 |
|---|---|---|---|
| seller_tax_id | 0.889 → **0.044** | 1.000 → **1.000** | 規則「找 8 位數字當統編」在 SROIE 上把 GST 編號/電話/收據序號全部誤判；VLM 理解「這是台灣統編的概念」，收據上沒有就回傳 null，不是矇對 |
| seller_name | 0.911 → **0.200** | 0.978 → 0.822 | 規則「取第一行當店名」是為合成圖生成方式量身訂做的捷徑，真實收據第一行常是浮水印/手寫註記/地址 |
| doc_type | 0.933 → 0.622 | 0.978 → **0.489** | VLM 在這欄位反而更差：SROIE 收據常印「TAX INVOICE」，模型語意上合理地判成 `e_invoice`，但 schema 定義的 `e_invoice` 專指台灣電子發票——這是 **schema 跨資料集失準**，不是模型或規則的錯 |

**結論句**：規則式管線的準確率高度依賴「訓練時設想的資料分布」（語言、格式、schema
定義），換一個分布幾乎等於重寫成本；VLM 的核心優勢不是準確率絕對值更高，而是
**同一份投資（prompt/schema）能不能撐住分布外的輸入**——這組數字直接量化了這個差異，
也誠實揭露了 VLM 不是萬能（doc_type 這個語意模糊的欄位它反而輸給規則）。

## 核心發現二：前處理與 OCR 輔助都不是無條件有益（合成繁中，按劣化類型拆解）

### 表頭欄位 exact match（配置 × 劣化類型）

| config | clean | fade | wrinkle | rotate | blur | stamp | handwriting |
|---|---|---|---|---|---|---|---|
| A 有前處理 | 1.00 | 1.00 | 0.98 | 1.00 | **0.14** | 0.96 | 1.00 |
| A 無前處理 | 1.00 | 1.00 | 1.00 | **0.76** | 0.96 | 1.00 | 1.00 |
| Qwen3-VL-8B | 0.98 | 1.00 | 0.86 | 1.00 | 0.96 | 1.00 | 1.00 |
| GPT-5.4-nano | 0.67 | 0.71 | 0.64 | 0.55 | 0.54 | 0.71 | 0.77 |
| GPT-5.4-nano+輔助 | 0.93 | 0.93 | 0.90 | 0.95 | 0.54 | 1.00 | 1.00 |

- **二值化在模糊圖上把 Pipeline A 帶向近乎全滅（1.00→0.14）**，但在旋轉圖上前處理是決定性的
  （0.76→1.00）。同一個「前處理」開關，對不同劣化類型的效果方向完全相反——不能一概而論
  「前處理有沒有用」，要按劣化類型分開看。
- **合理推測**：深度學習 OCR/VLM 都是在自然灰階/彩色影像上訓練的，強制二值化把模糊圖的輸入
  推出訓練分布之外；旋轉圖則相反——deskew 保護的是 `layout.py` 依座標分行的邏輯，不做的話
  結構性錯位（品名跟金額被誤合併成假品項），這也是為何 Pipeline A 在合成資料上 items F1
  只有 0.58~0.61，明顯低於 VLM 的 0.97——品項欄位對版面錯位的敏感度最高。
- **OCR 輔助文字對 gpt-5.4-nano 是穩定的正面因子**（几乎每個劣化類型都上升，clean
  0.67→0.93），但**對 qwen3-vl 是雙面刃**（clean 0.98→0.98 持平，rotate 反而 1.00→0.88
  略降）——同一個「給輔助資訊」的介入，對不同模型的效果不一致，必須逐 backend 驗證，
  不能假設「多給資訊只有好處」。

## 逐張質化分析（旋轉 sample_002 的失敗模式對比）

**旋轉對兩條管線造成完全不同量級的失敗**：Pipeline A 沒有 deskew 時是**結構性錯位**
——`layout.py` 依 y 座標分行被打散，「品名」這種表頭字樣跟另一列的金額被誤判成同一列，
產生「品名：60」這種假品項，總計欄位也一併抓空，一步錯、步步錯。VLM 對旋轉的降級是
**局部字元誤讀**——多數欄位仍抽對，只有品項名稱裡的個別字（如「鮪」被讀成「鯖/鯡/鱈」）
被誤讀成形似字，不會結構性坍塌。

**items F1 的度量盲點**：即使 F1 看起來高，`items_name_exact_rate` 揭露 qwen3-vl 兩種模式
都只有 0.92~0.95——代表約 5~8% 配對成功的品項，名稱本身其實有錯字，只是相似度仍高於
對齊門檻而已。單看 F1 會誤以為品項抽取「完美」。

## 什麼場景該用哪條管線

**傾向 Pipeline A（傳統 OCR 管線）**：
- 大量文件、成本高度敏感——CPU 本地運算邊際成本趨近於零。
- 需要完全離線/無法連外部 API 的部署環境。
- 需要規則邏輯可審計、可解釋。
- **文件語言/格式高度單一且已針對該格式調過規則**——這是最關鍵的前提，可移植性數據顯示
  一旦格式漂移（哪怕只是換個國家的類似文件），準確率會斷崖式下跌。

**傾向 Pipeline B（端到端 VLM）**：
- 文件來源多樣（不同語言、不同版面樣板、不同國家）——可移植性是 VLM 壓倒性的優勢，
  同一個 prompt 不用重寫就能處理分布外的輸入。
- 影像來源不可控、品質參差不齊——但**不是無腦選 VLM**：本地 qwen3-vl 準確率最穩，
  便宜檔 gpt-5.4-nano 在合成繁中資料上明顯較弱（0.66，需靠 OCR 輔助拉到 0.91），
  便宜不代表夠用，要依場景實測。
- 可以接受較高延遲與 API/GPU 成本，換取準確率與可移植性。

**兩條管線的共同弱點**：品項名稱這種細粒度文字兩者都有非零錯誤率；doc_type 這類「語意
邊界模糊」的欄位，VLM 不保證比規則準（SROIE 上 VLM 的 doc_type 甚至更差）——**任何欄位
上線前都該用其嚴格指標（`name_exact_rate`、逐欄位 exact）驗證，而非只看整體平均**。

## 已知限制

- 手寫字（合成資料集用標楷體模擬手寫感，非真實手寫）、印章遮擋（合成資料集用半透明橢圓疊圖
  模擬）皆為工程化近似，不是真實手寫筆跡/真實印泥的影像特性；真實效度介於「完全沒測」與
  「真實資料」之間。
- Gemini 3.5 Flash 因免費方案配額（20 次/天）未能納入本次正式雙軌評估，配額問題與診斷過程
  記錄於 `plan.md`。
- SROIE 無品項標註，items 相關指標僅來自合成繁中資料集。
